void main() {
  var isRaining = true;

  print('Prepare before going iffice');
  if (isRaining) {
    print('Bawa payung');
  }
  print('going to the office');
}

// ekpresi if akan menguji suatu kondisi. Jika hasil ekspresi bernilai true, maka blok kode di dalamnya
// akan di jalankan. jika bernilai false maka proses yang di tentukan akan di lewatkan;
